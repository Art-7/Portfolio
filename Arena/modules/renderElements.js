class nameplate {
    constructor(fieldName, fieldLevel, currentHP, maxHP) {
        this.fieldName = fieldName;
        this.fieldLevel = fieldLevel;
        this.currentHP = currentHP;
        this.maxHP = maxHP;
    }

    generateNameplate()
}